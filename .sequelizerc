const path = require("path");
const basePath = path.resolve("./server/services");

module.exports = {
  config: path.join(basePath, "/config/config.js"),
  "seeders-path": path.join(basePath, "seeders"),
  "migrations-path": path.join(basePath, "migration"),
};
